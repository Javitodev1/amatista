---
import Layout from '@/layouts/Layout.astro'
import SectionContainer from '@/components/SectionContainer.astro'
import ShopRC from '@/react-components/Shop'
import products from '@/data/products.json'
interface Props {
  initFilter?: string
}

const { initFilter = 'todo' } = Astro.props
---

<Layout title="Tienda Amatista - Second hand shop | Indumentaría">
  <SectionContainer>
    <ShopRC
      data={products}
      initFilter={initFilter}
      client:load
    />
  </SectionContainer>
</Layout>
